{
  "scripts": {
    "start": "json-server data/db.json -m ./node_modules/json-server-auth -r data/routes.json --port 8000"
  },
  "dependencies": {
    "json-server": "^0.17.4",
    "json-server-auth": "^2.1.0"
  }
}
